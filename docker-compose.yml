services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: timer
      POSTGRES_PASSWORD: timer
      POSTGRES_DB: timer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
